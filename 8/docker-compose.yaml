name: projob5

services:
  backend:
    build:
      context: .
      dockerfile: ./server/Dockerfile
    restart: always
    ports:
      - "8000:8000"
  frontend:
    build:
      context: .
      dockerfile: ./Dockerfile
    restart: always
    ports:
      - "8001:80"
